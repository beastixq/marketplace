% !TEX root = ../report.tex

\chapter{Технологическая часть}

В данном разделе описывается реализация базы данных и программного интерфейса доступа к ней. Рассматриваются средства реализации, порядок применения миграций, реализованные ограничения целостности, роли СУБД, триггеры, хранимая функция, Redis-кэширование и REST-интерфейс.

\section{Выбор средств реализации}

Для реализации базы данных используется PostgreSQL 16. PostgreSQL относится к реляционным СУБД и поддерживает транзакции, внешние ключи, ограничения целостности, частичные индексы, пользовательские роли, триггеры и хранимые функции на языке PL/pgSQL~\cite{postgresql_doc}. Эти возможности соответствуют требованиям проектируемой системы: данные маркетплейса имеют выраженную табличную структуру, а часть правил целостности должна выполняться непосредственно на уровне базы данных.

Выбор СУБД выполнен по критериям, связанным с реализуемой схемой. В качестве альтернатив рассматривались PostgreSQL, MySQL и SQLite~\cite{postgresql_doc,mysql_doc,sqlite_doc}. Сравнение приведено в таблице~\ref{tbl:dbms_compare}.

\begin{table}[H]
    \caption{Сравнение СУБД по требованиям реализации}
    \label{tbl:dbms_compare}
    \begin{tabular}{|p{0.36\textwidth}|c|c|c|}
        \hline
        \textbf{Критерий} & \textbf{PostgreSQL} & \textbf{MySQL} & \textbf{SQLite} \\ \hline
        Серверный многопользовательский режим & да & да & нет \\ \hline
        Роли и привилегии на уровне СУБД & да & да & нет \\ \hline
        Триггеры и хранимые функции & да & да & частично \\ \hline
        Частичные индексы & да & нет & да \\ \hline
        Строгие типы для денежных и временных значений & да & да & частично \\ \hline
        Поддержка внешних ключей и транзакций & да & да & да \\ \hline
    \end{tabular}
\end{table}

По результатам сравнения PostgreSQL покрывает все перечисленные требования. Для данной работы существенными являются роли СУБД, частичные индексы, триггеры и функция, возвращающая табличный результат. Поэтому в реализации используется PostgreSQL.

Серверное приложение реализовано на языке Go. Язык используется для реализации HTTP-интерфейса, бизнес-операций, транзакционных сценариев и взаимодействия с PostgreSQL и Redis. В проекте применяется модуль Go версии 1.25.0~\cite{go_doc}. Для работы с PostgreSQL используется драйвер \texttt{pgx/v5}, для маршрутизации HTTP-запросов --- библиотека \texttt{chi}, для построения SQL-запросов --- библиотека \texttt{squirrel}.

Миграции схемы выполняются утилитой \texttt{goose}~\cite{goose_doc}. Каждая миграция содержит секции \texttt{Up} и \texttt{Down}, что позволяет воспроизводимо применять и откатывать изменения схемы. Такой подход фиксирует порядок создания таблиц, ограничений, индексов, ролей, триггеров и функций.

Redis используется как InMemory-СУБД для кэширования часто читаемых данных и для хранения списка отозванных JWT-токенов~\cite{redis_doc}. PostgreSQL остаётся источником истины; Redis используется только как дополнительный слой ускорения чтения и проверки сессий.

Основные средства реализации приведены в таблице~\ref{tbl:tech_tools}.

\begin{table}[H]
    \caption{Средства реализации}
    \label{tbl:tech_tools}
    \begin{tabular}{|p{0.24\textwidth}|p{0.28\textwidth}|p{0.38\textwidth}|}
        \hline
        \textbf{Средство} & \textbf{Используемая версия или пакет} & \textbf{Назначение} \\ \hline
        PostgreSQL & 16.11 & хранение данных, ограничения целостности, роли, индексы, триггеры и функции \\ \hline
        Redis & 8.6 & кэширование read-моделей и хранение идентификаторов отозванных JWT \\ \hline
        Go & 1.25.0 & реализация серверного приложения и интерфейса доступа к БД \\ \hline
        pgx & \texttt{pgx/v5} & подключение к PostgreSQL и выполнение SQL-запросов \\ \hline
        chi & \texttt{chi/v5} & маршрутизация HTTP-запросов \\ \hline
        goose & \texttt{goose} & применение миграций схемы БД \\ \hline
        Docker Compose & файл \texttt{docker-compose.yml} & запуск PostgreSQL и Redis в локальной среде \\ \hline
    \end{tabular}
\end{table}

\section{Реализация базы данных}

Схема базы данных создаётся набором миграций, расположенных в каталоге \texttt{migrations}. Порядок миграций приведён в таблице~\ref{tbl:tech_migrations}.

\begin{table}[H]
    \caption{Миграции базы данных}
    \label{tbl:tech_migrations}
    \begin{tabular}{|p{0.18\textwidth}|p{0.74\textwidth}|}
        \hline
        \textbf{Миграция} & \textbf{Назначение} \\ \hline
        \texttt{001} & создание основных таблиц: пользователей, продавцов, товаров, категорий, заказов, позиций заказов, адресов, отзывов и истории изменения цен \\ \hline
        \texttt{002} & создание индексов для выборок товаров, заказов, отзывов, категорий и активных записей \\ \hline
        \texttt{003} & создание ролей PostgreSQL и назначение привилегий \\ \hline
        \texttt{004} & создание триггера аудита изменений цен \\ \hline
        \texttt{005} & создание хранимой функции статистики продавца \\ \hline
        \texttt{006} & добавление связи заказа с продавцом \\ \hline
        \texttt{007} & добавление рейтинга товара и триггера пересчёта рейтингов товара и продавца \\ \hline
        \texttt{008} & создание системных учётных записей администратора и аналитика \\ \hline
        \texttt{009} & добавление учёта зарезервированного количества товара \\ \hline
        \texttt{010} & добавление полей дома и квартиры в адрес доставки \\ \hline
        \texttt{011} & добавление инвариантов корзины: одна draft-корзина на покупателя и одна позиция товара в заказе \\ \hline
        \texttt{012} & снятие уникальности с номера телефона пользователя \\ \hline
    \end{tabular}
\end{table}

Для применения миграций используется команда:

\begin{lstlisting}[language={}, caption={Команда применения миграций}]
goose -dir migrations postgres "$DATABASE_URL" up
\end{lstlisting}

Итоговая схема содержит десять таблиц. Таблицы и ограничения были описаны в конструкторской части в таблицах~\ref{tbl:t_users}--\ref{tbl:t_price_history}. В технологической реализации сохраняются следующие группы ограничений:

\begin{itemize}
    \item ограничения домена значений: положительная цена товара, неотрицательное количество товара, допустимый диапазон оценки отзыва, фиксированный набор ролей и статусов заказа;
    \item ограничения ссылочной целостности: связи товара с продавцом, заказа с покупателем, позиции заказа с заказом и товаром, отзыва с пользователем и товаром;
    \item ограничения уникальности: электронная почта пользователя, профиль продавца на пользователя, имя категории, пара заказа и товара в позиции заказа, пара пользователя и товара в отзыве;
    \item частичный уникальный индекс для ограничения одной draft-корзины на покупателя;
    \item проверка \texttt{reserved\_quantity <= stock\_quantity}, исключающая резервирование количества больше доступного остатка.
\end{itemize}

Для мягкого удаления пользователей и товаров используются столбцы \texttt{deleted\_at}. Поиск активных пользователей и товаров поддерживается частичными индексами \texttt{idx\_users\_id\_not\_deleted} и \texttt{idx\_products\_id\_not\_deleted}. Эти индексы включают только строки, у которых \texttt{deleted\_at IS NULL}.

\section{Реализация ролевой модели}

На уровне PostgreSQL реализованы четыре роли:

\begin{itemize}
    \item \texttt{marketplace\_buyer};
    \item \texttt{marketplace\_seller};
    \item \texttt{marketplace\_admin};
    \item \texttt{marketplace\_analyst}.
\end{itemize}

Роли создаются миграцией \texttt{003\_create\_roles.sql}. Привилегии назначаются командами \texttt{GRANT}.

Роль покупателя получает права чтения каталога и категорий, а также права изменения адресов, отзывов, заказов и позиций заказа. Роль продавца получает права управления товарами и чтения заказов, связанных с его товарами. Роль администратора получает полный доступ к таблицам и последовательностям. Роль аналитика получает доступ только на чтение.

Роли PostgreSQL задают технический уровень доступа к объектам БД. Правила принадлежности отдельных строк проверяются в приложении при выполнении пользовательских операций. Например, покупатель работает только со своими адресами, заказами и отзывами, а продавец --- со своим профилем, своими товарами и заказами, относящимися к этим товарам.

\section{Реализация триггеров и хранимой функции}

В базе данных реализованы два триггера и одна хранимая функция. Их назначение соответствует проектным решениям, описанным в конструкторской части.

Триггер \texttt{catch\_price\_change} создаётся миграцией \texttt{004}. Он срабатывает после изменения столбца \texttt{price} в таблице \texttt{products}. Условие \texttt{OLD.price IS DISTINCT FROM NEW.price} исключает запись в журнал при отсутствии фактического изменения цены. При срабатывании триггера функция аудита добавляет строку в таблицу \texttt{product\_price\_history}.

Триггер \texttt{update\_ratings\_on\_review} создаётся миграцией \texttt{007}. Он срабатывает после добавления отзыва, изменения оценки или удаления отзыва. Функция пересчёта рейтингов обновляет рейтинг товара как среднее значение оценок по этому товару. Затем пересчитывается рейтинг продавца как среднее значение рейтингов его активных товаров.

Хранимая функция \texttt{get\_seller\_statistics} создаётся миграцией \texttt{005}. Функция принимает идентификатор продавца и границы периода. В результат входят количество заказов, суммарная выручка, средний чек и наименование самого продаваемого товара. В расчёт включаются только заказы со статусами \texttt{paid}, \texttt{shipped} и \texttt{delivered}. Цена берётся из столбца \texttt{price\_at\_purchase}, поэтому изменение текущей цены товара не меняет статистику уже оформленных заказов.

Вызов функции выполняется запросом:

\begin{lstlisting}[language=SQL, caption={Вызов функции статистики продавца}]
SELECT total_orders, total_revenue, avg_order_value, top_product_name
FROM get_seller_statistics($1, $2, $3);
\end{lstlisting}

\section{Тестирование хранимой функции}

Для проверки функции \texttt{get\_seller\_statistics} выделены классы эквивалентности входных данных. Классы приведены в таблице~\ref{tbl:stats_equiv}.

\begin{table}[H]
    \caption{Классы эквивалентности для тестирования функции \texttt{get\_seller\_statistics}}
    \label{tbl:stats_equiv}
    \begin{tabular}{|p{0.08\textwidth}|p{0.35\textwidth}|p{0.45\textwidth}|}
        \hline
        \textbf{Класс} & \textbf{Условие} & \textbf{Ожидаемый результат} \\ \hline
        K1 & у продавца нет завершённых продаж в заданном периоде & \texttt{total\_orders = 0}; агрегаты выручки, среднего чека и лидирующего товара не формируются \\ \hline
        K2 & у продавца есть один оплаченный заказ с одной позицией & количество заказов равно 1; выручка равна произведению количества на \texttt{price\_at\_purchase}; средний чек равен выручке \\ \hline
        K3 & один заказ содержит несколько позиций товаров продавца & заказ учитывается один раз; выручка суммируется по всем позициям \\ \hline
        K4 & в период попадают заказы со статусами \texttt{draft}, \texttt{pending} или \texttt{cancelled} & такие заказы исключаются из расчёта \\ \hline
        K5 & часть заказов находится вне заданного временного интервала & заказы вне интервала не влияют на агрегаты \\ \hline
        K6 & продано несколько товаров с разным количеством единиц & \texttt{top\_product\_name} соответствует товару с максимальной суммой \texttt{quantity} \\ \hline
    \end{tabular}
\end{table}

Контрольный запрос для проверки функции имеет следующий вид:

\begin{lstlisting}[language=SQL, caption={Контрольный запрос к функции статистики}]
SELECT *
FROM get_seller_statistics(1, DATE '2026-01-01', DATE '2026-02-01');
\end{lstlisting}

Проверка должна выполняться на тестовых данных, содержащих заказы разных статусов, несколько товаров одного продавца и заказы за пределами выбранного периода. Такой набор данных позволяет проверить фильтрацию по статусу, фильтрацию по дате, подсчёт уникальных заказов и выбор лидирующего товара.

\section{Кэширование с использованием Redis}

Для часто повторяемых чтений используется стратегия Cache-Aside. При чтении приложение сначала обращается к Redis. При наличии значения выполняется десериализация JSON и результат возвращается вызывающему коду. При отсутствии значения приложение обращается к PostgreSQL, получает актуальные данные, записывает их в Redis с TTL и возвращает результат.

PostgreSQL остаётся источником истины. Redis не используется для хранения корзин, заказов, платежей, профилей и отчётов. Эти данные зависят от пользователя, часто изменяются или требуют строгой актуальности.

Схема взаимодействия PostgreSQL и Redis приведена на рисунке~\ref{fig:redis_interaction}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=stealth, font=\small]
        \node[draw, rectangle, align=center, minimum width=3.4cm, minimum height=1cm] (client) at (0, 3.2) {HTTP-клиент};
        \node[draw, rectangle, align=center, minimum width=4.2cm, minimum height=1cm] (app) at (0, 1) {Go-приложение\\service/repository};
        \node[draw, rectangle, align=center, minimum width=3.2cm, minimum height=1cm] (redis) at (8.1, 1) {Redis\\кэш};
        \node[draw, rectangle, align=center, minimum width=3.6cm, minimum height=1cm] (pg) at (0, -2.1) {PostgreSQL\\источник истины};

        \draw[->] (client) -- node[right] {1. HTTP} (app);
        \draw[->] ([yshift=0.2cm]app.east) -- node[above] {2. GET} ([yshift=0.2cm]redis.west);
        \draw[->] ([yshift=-0.2cm]redis.west) -- node[below] {3. hit/miss} ([yshift=-0.2cm]app.east);
        \draw[->] (app.north east) to[out=35,in=145] node[above] {6. SET TTL} (redis.north west);
        \draw[->] (app.south east) to[out=-35,in=-145] node[below] {7. DEL/SCAN} (redis.south west);

        \draw[->] ([xshift=-0.45cm]app.south) -- node[left] {4. SQL} ([xshift=-0.45cm]pg.north);
        \draw[->] ([xshift=0.45cm]pg.north) -- node[right] {5. данные} ([xshift=0.45cm]app.south);
    \end{tikzpicture}
    \caption{Схема взаимодействия PostgreSQL и Redis}
    \label{fig:redis_interaction}
\end{figure}

Ключи Redis приведены в таблице~\ref{tbl:redis_keys}.

\begin{table}[H]
    \caption{Ключи Redis}
    \label{tbl:redis_keys}
    {\small
    \begin{tabular}{|p{0.38\textwidth}|p{0.16\textwidth}|p{0.34\textwidth}|}
        \hline
        \textbf{Ключ} & \textbf{TTL} & \textbf{Назначение} \\ \hline
        \texttt{products:\{id\}} & \texttt{product\_ttl} & карточка товара \\ \hline
        \texttt{products:catalog:\{query\}} & \texttt{catalog\_ttl} & страница каталога с параметрами фильтрации, сортировки и пагинации \\ \hline
        \texttt{categories:list:\{query\}} & \texttt{category\_ttl} & список категорий \\ \hline
        \makecell[l]{\texttt{products:\{id\}:reviews:}\\\texttt{page=\{p\}\&limit=\{l\}}} & \texttt{review\_ttl} & страница отзывов товара \\ \hline
        \texttt{sessions:\{jti\}} & срок действия JWT & список отозванных токенов \\ \hline
    \end{tabular}
    }
\end{table}

Параметры запроса включаются в ключ в виде канонической query-string строки. Категории сортируются перед формированием ключа, параметры кодируются средствами URL-encoding. Например, для первой страницы каталога с сортировкой по возрастанию цены используется ключ вида \texttt{products:catalog:limit=12\&page=1\&sort=asc}.

Инвалидация выполняется в декораторах репозиториев. Для удаления множества ключей по префиксу используется команда \texttt{SCAN}, а не \texttt{KEYS}. Это снижает риск блокировки Redis при большом количестве ключей. Если мутация выполняется внутри транзакции PostgreSQL, очистка кэша регистрируется как действие после успешного \texttt{COMMIT}. Такой порядок уменьшает вероятность повторной записи устаревших данных в кэш до фиксации изменений в БД.

Изменение остатка товара инвалидирует ключ \texttt{products:\{id\}}, но не инвалидирует весь каталог. Каталог может показывать остаток с задержкой до \texttt{catalog\_ttl}. Точная проверка доступного количества выполняется при операциях корзины и оформления заказа в PostgreSQL.

Redis также используется для хранения списка отозванных JWT. При выходе пользователя из системы в Redis записывается ключ \texttt{sessions:\{jti\}} со сроком жизни, равным оставшемуся времени действия токена. При недоступности Redis во время проверки токена защищённый запрос отклоняется. Такой режим отказа используется для того, чтобы отозванный токен не был принят при сбое InMemory-слоя.

\section{Интерфейс доступа к базе данных}

Доступ к данным реализован через JSON REST API. Маршруты имеют префикс \texttt{/api/v1}. Формат запросов и ответов --- JSON. Для защищённых маршрутов используется JWT, передаваемый в заголовке \texttt{Authorization: Bearer <token>}. Ошибки возвращаются в виде объекта \texttt{\{"error": "message"\}}.

Группы маршрутов программного интерфейса приведены в таблице~\ref{tbl:api_groups}. HTTP-семантика методов соответствует стандартному назначению операций чтения, создания, обновления и удаления ресурсов~\cite{http_semantics}.

\begin{table}[H]
    \caption{Группы маршрутов программного интерфейса REST}
    \label{tbl:api_groups}
    {\small
    \begin{tabular}{|p{0.22\textwidth}|p{0.42\textwidth}|p{0.24\textwidth}|}
        \hline
        \textbf{Группа} & \textbf{Примеры маршрутов} & \textbf{Назначение} \\ \hline
        Авторизация & \makecell[l]{\texttt{POST /auth/register}\\\texttt{POST /auth/login}\\\texttt{POST /auth/logout}} & регистрация, вход и отзыв текущего токена \\ \hline
        Пользователи & \makecell[l]{\texttt{GET /users/me}\\\texttt{PATCH /users/me}} & просмотр и изменение собственного профиля \\ \hline
        Каталог & \makecell[l]{\texttt{GET /products}\\\texttt{GET /products/\{id\}}\\\texttt{GET /categories}} & чтение товаров, карточек товаров и категорий \\ \hline
        Товары продавца & \makecell[l]{\texttt{POST /products}\\\texttt{PATCH /products/\{id\}}\\\texttt{DELETE /products/\{id\}}} & создание, изменение и мягкое удаление товаров \\ \hline
        Корзина и заказы & \makecell[l]{\texttt{GET /cart}\\\texttt{POST /cart/items}\\\texttt{POST /orders}} & работа с draft-корзиной и оформление заказа \\ \hline
        Оплата & \makecell[l]{\texttt{POST /orders/\{id\}/}\\\texttt{payment-link}\\\texttt{POST /payments/callback/}\\\texttt{mock-bank}} & получение платёжной ссылки и обработка callback mock-bank \\ \hline
        Отзывы & \makecell[l]{\texttt{GET /products/\{id\}/reviews}\\\texttt{POST /reviews}\\\texttt{PATCH /reviews/\{id\}}} & чтение, создание и изменение отзывов \\ \hline
        Администрирование & \makecell[l]{\texttt{GET /admin/users}\\\texttt{DELETE /admin/users/\{id\}}\\\texttt{POST /categories}} & управление пользователями, продавцами, отзывами и категориями \\ \hline
    \end{tabular}
    }
\end{table}

Помимо JSON API, в приложении реализован серверный Web-интерфейс. Он использует те же сервисные операции, что и API, но формирует HTML-страницы средствами \texttt{html/template}. Наличие Web-интерфейса не изменяет схему БД: оба интерфейса работают с одними и теми же таблицами через общий слой операций.

\section{Запуск и проверка реализации}

Локальная среда запускается с помощью Docker Compose:

\begin{lstlisting}[language={}, caption={Запуск PostgreSQL и Redis}]
docker compose up -d
\end{lstlisting}

После запуска контейнеров задаётся строка подключения к базе данных и применяются миграции:

\begin{lstlisting}[language={}, caption={Применение миграций в локальной среде}]
export DATABASE_URL='postgres://postgres:postgres@localhost:5432/marketplace?sslmode=disable'
goose -dir migrations postgres "$DATABASE_URL" up
\end{lstlisting}

Серверное приложение запускается командой:

\begin{lstlisting}[language={}, caption={Запуск серверного приложения}]
go run ./cmd/api -config config/config.yaml
\end{lstlisting}

Для проверки компиляции, модульных тестов и интеграционных тестов используется команда:

\begin{lstlisting}[language={}, caption={Запуск тестов}]
go test ./...
\end{lstlisting}

При наличии переменной окружения \texttt{DATABASE\_URL} интеграционные тесты репозиториев выполняются с использованием PostgreSQL. Тесты кэша поднимают локальный процесс Redis и проверяют формирование ключей, cache hit, отсутствие negative-cache, инвалидацию по префиксу и срок жизни ключей отозванных токенов.

\section*{Вывод}
\addcontentsline{toc}{section}{Вывод}

В технологической части были описаны средства реализации, порядок создания схемы БД, реализованные ограничения целостности, роли PostgreSQL, триггеры, хранимая функция статистики продавца, тестовые классы для функции, Redis-кэширование и REST-интерфейс доступа к данным. Реализация сохраняет PostgreSQL в качестве основного хранилища данных, а Redis используется как вспомогательный слой для ускорения повторных чтений и проверки отозванных токенов.
